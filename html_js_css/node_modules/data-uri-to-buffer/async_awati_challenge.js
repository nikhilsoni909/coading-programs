import { resolve } from "path";

function resolveafter3second() {
    return new promise(resolve => {

        setTimeout(() => {
            resolve("resolved ")
        }, 3000)
    });
}
// resolveafter3second().then((data) => {
//     console.log(data);
// });
async fucntion(getasyncData()){
    const result = await resolveafter3second();
    console.log(result);
}
getasyncdata(); 